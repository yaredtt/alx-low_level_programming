#include "main.h"
#include <stdio.h>
/**
 * *_strcat - concatinate two strings
 * @d: destination string
 * @s: source string
 * Return:
 */
char *_strcat(char *dest, char *src)
{
	char str1[];
	char str2[];
	int len;
	int i;

	strcat(str1, str2);
	len = strlen(str1);
	while (i <= len)
{
	_putchar(str1[]);
	i++;
}
	_putchar('\n');
}
